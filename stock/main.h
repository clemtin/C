#ifndef MAIN_H
#define MAIN_H

#include <stdlib.h>
#include <stdio.h>

typedef struct pomme * tpomme;
typedef struct palette * tpalette;
typedef struct buchel * tbuchel;







extern void init;










#endif